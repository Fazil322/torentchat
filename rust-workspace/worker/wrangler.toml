name = "torentchat-worker"
main = "build/worker/shim.mjs"
compatibility_date = "2024-07-01"
compatibility_flags = ["nodejs_compat"]

# No [build] section — we build manually in CI (cargo build + wasm-bindgen)
# This prevents wrangler from re-running worker-build on deploy.
