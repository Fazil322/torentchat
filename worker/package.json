{
  "name": "torentchat-worker",
  "version": "0.1.0",
  "description": "TorentChat signaling & ephemeral KV cache relay. Serverless, edge, E2E-encrypted. Never stores plaintext.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
